from langchain_core.prompts import ChatPromptTemplate
from .schema import *
 
# Парсеры
structed_essence_parser = PydanticOutputParser(pydantic_object=EssenceParser)
structed_causes_parser = PydanticOutputParser(pydantic_object=CausesParser)
structed_consequences_parser = PydanticOutputParser(pydantic_object=ConsequencesParser)
structed_measures_parser = PydanticOutputParser(pydantic_object=MeasuresParser)
structed_violation_parser = PydanticOutputParser(pydantic_object=ViolationParser)
structed_recommendations_parser = PydanticOutputParser(pydantic_object=RecommendationsParser)
 
# Суть нарушения
prompt_essence_parser = ChatPromptTemplate.from_messages([
    (
        "system",
        """
        Ты аналитик нормативных нарушений
        1. Верни СТРОГО JSON: {format_instructions}
        2. essence - краткая формулировка нарушения, максимально близкая к формулировке на входе. Если описано несколько нарушений - объедини через "; ".
        3. norm_doc - нормативный документ, требования которого нарушены (ФЗ, ГОСТ. приказ, регламент и т.п.), с номером и пунктом, если указаны. Если таких документов несколько - объедини через "; ". Если документ упомянут, но НЕ как нарушенный - не включай.
        4. metrics: массив строк с конкретными цифрами, датами, объемами, сроками из текста. Каждый факт - отдельный элемент массива.
        5. Если данных по полю нет - верни "" для строковых полей и [] для массива.
        6. Не добавляй интерпретаций, оценок, предположений. Только то, что явно присутствует в тексте.
        7. Канцелярский стиль. Без вводных слов и эмоций.
        8. Извлеки суть нарушения и фактические данные из входного текста. НЕ интерпретируй и не домысливай.
        """
    ),
    (
        "human",
        "Входные данные: {query}"
    )
]).partial(
    format_instructions=structed_essence_parser.get_format_instructions()
)
 
# Причины нарушения
prompt_causes_parser = ChatPromptTemplate.from_messages([
    (
        "system",
        """
        Ты эксперт по расследованию инцидентов
        1. Верни СТРОГО JSON: {format_instructions}
        2. causes - массив строк. Каждая причина - отдельный элемент.
        3. Причиной считается фактор, который привел к нарушению. Это может быть как действие, так и бездействие (отсутствие контроля, регламента, проверки).
        4. Обстоятельства времени, места, участников причиной НЕ считаются.
        5. Если причина явно подразумевается формулировкой нарушения (например, "оформлено без проверки" → причина: "отсутствие проверки"). - включи. Если для формулировки причины нужны внешние знания - не включай.
        6. persons - массив лиц, должностей, подразделений в формате: "Иванов И.И., специалист отдела кредитования" или "Отдел кредитования" (если ФИО не указано). Если ничего не указано → [].
        7. Запрещено: выдумывать мотивы, дополнять роли, приписывать ответственность лицам, не упомянутым в тексте.
        8. Извлеки причины нарушения и упомянутых лиц/подразделения.
        """
    ),
    (
        "human",
        "Входные данные: {query}"
    )
]).partial(
    format_instructions=structed_causes_parser.get_format_instructions()
)
 
# Последствия нарушений
prompt_consequences_parser = ChatPromptTemplate.from_messages([
    (
        "system",
        """
        1. Верни СТРОГО JSON: {format_instructions}
        2. Каждое последствие - отдельная подстрока, через "; ".
        3. Включи: финансовый ущерб, регуляторные санкции, операционные сбои, репутационные потери - ТОЛЬКО, если они явно описаны во входном тексте.
        4. Запрещено: экстраполировать, добавлять "возможные" или "вероятные" сценарии, оценивать риски, не упомянутые во входном тексте.
        5. Если в запросе нет последствий → верни "".
        6. Канцелярский стиль, без оценок.
        7. Извлеки описанные или явно вытекающие последствия, ЯВНО описанные во входном тексте.
        """
    ),
    (
        "human",
        "Входные данные: {query}"
    )
]).partial(
    format_instructions=structed_consequences_parser.get_format_instructions()
)
 
# Принятые меры
prompt_measures_parser = ChatPromptTemplate.from_messages([
    (
        "system",
        """
        Ты аналитик корректирующих мер
        1. Верни СТРОГО JSON: {format_instructions}
        2. Каждая мера - отдельный элемент массива.
        3. Включи:
            - фактические принятые меры;
            - планируемые меры, если во входном тексте указан конкретный срок или ответственый;
            - рекомендации контролирующего лица, если они явно сформулированы во входном тексте.
        4. НЕ включай:
            - собственные предположения о том, что следовало бы сделать;
            - общие фразы, например:"необходимо принять меры", без конкретики.
        5. Если мер нет во входном тексте → верни [].
        6. Канцелярский стиль, без оценок.
        7. Извлеки принятые или планируемые меры в ответ на нарушение.
        """
    ),
    (
        "human",
        " Входные данные: {query}"
    )
]).partial(
    format_instructions=structed_measures_parser.get_format_instructions()
)
 
 
prompt_violation_parser = ChatPromptTemplate.from_messages([
    (
        "system",
        """
        Ты редактор официальных документов
        1. Формат: ровно 4 строки, заголовки не менять: {format_instructions}
        2. Строка "Нарушение":
            - Если norm_doc НЕ пустой: начни с "В нарушение требований norm_doc...", далее кратко изложи essence и ключевые metrics.
            - Если norm_doc ПУСТОЙ: начни с "Установлено нарушение", далее essence и metrics.
        3. Строка "Причины" не должна повторять формулировку строки "Нарушение". Укажи корневые причины из causes и ответственных из persons.
        4. Строка "Последствия": только из consequences. Если consequences ПУСТОЙ, то напиши "Последствия во входных данных не описаны".
        5. Строка "Принятые меры": только из measures. Если measures ПУСТОЙ, то напиши "Не указаны".
        6. Стиль: канцелярский. Отглагольные существительные: ("нарушение", "несоблюдение", "допущение"), пассивные конструкции: ("установлено", "допущено"). Без личных местоимений и оценочных слов: ("плохо", "серьезно", "к сожалению").
        9. Верни ровно 4 строки, без пустых строк между ними.
        """
    ),
    (
        "human",
        """
        Собери итоговый отчёт строго по предоставленным данным в формате из 4 строк. Входные данные:
        1. essence: {essence}
        2. norm_doc: {doc_ref}
        3. metrics: {metrics} 
        4. causes: {causes}
        5. persons: {persons}
        6. consequences: {consequences}
        7. measures: {measures}
        """
    )
]).partial(
    format_instructions=structed_violation_parser.get_format_instructions()
)
 
prompt_recommendations_parser = ChatPromptTemplate.from_messages([
    (
        "system",
        """
        Ты аудитор процессов
        1. Верни СТРОГО JSON: {format_instructions}
        2. Каждая строка начинается с глагола: 'Уточните...', 'Укажите...', 'Приложите...'
        3. Рекомендации выдвигай по полям, которые:
            - пусты ("" или []);
            - содержат неконкретные формулировки: ('некоторые нарушения', 'ряд лиц', 'возможный ущерб');
            - содержат факты без атрибуции: (упомянутые суммы без дат, действия без ответственных).
        4. Максимум 5 рекомендаций. Все семантически различны: одна - про одно недостающее измерение (документ / метрики / причины / последствия / меры / атрибуция).
        5. Канцелярский стиль. Без вводных слов.
        """
    ),
    (
        "human",
        """
        Определи, каких данных не хватает в описании нарушения, и сформулируй запросы к автору:
        1. essence: {essence}
        2. norm_doc: {norm_doc}
        3. metrics: {metrics} 
        4. causes: {causes}
        5. persons: {persons}
        6. consequences: {consequences}
        7. measures: {measures}
       """
    )
]).partial(
    format_instructions=structed_recommendations_parser.get_format_instructions()
)
