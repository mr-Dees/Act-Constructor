{
  "name": "act-constructor-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E suite для Act Constructor (vanilla JS фронт)",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:debug": "playwright test --debug",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
