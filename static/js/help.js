/**
 * Менеджер системы помощи и инструкций
 * Управляет отображением инструкций для пользователя на разных этапах работы с приложением
 */
class HelpManager {
    /**
     * Словарь инструкций для каждого шага работы с конструктором актов
     * Ключ - номер шага, значение - объект с заголовком и HTML-содержимым инструкции
     */
    static instructions = {
        1: {
            title: 'Инструкция: Шаг 1 - Составление структуры акта',
            content: `
                <h4>📋 Создание структуры документа</h4>
                <p>На этом шаге вы формируете иерархическую структуру акта - добавляете разделы, подразделы и элементы содержания.</p>
                
                <h4>🔧 Основные действия:</h4>
                <ul>
                    <li><strong>Правый клик на элементе</strong> - открывает контекстное меню с доступными действиями</li>
                    <li><strong>Добавить дочерний элемент</strong> - создаёт подраздел внутри выбранного элемента</li>
                    <li><strong>Добавить соседний элемент</strong> - создаёт элемент на том же уровне</li>
                    <li><strong>Добавить таблицу</strong> - вставляет таблицу как дочерний элемент</li>
                    <li><strong>Добавить текстовый блок</strong> - добавляет блок для произвольного текста</li>
                    <li><strong>Добавить нарушение</strong> - создаёт структурированный блок описания нарушения</li>
                    <li><strong>Удалить элемент</strong> - удаляет выбранный элемент и всех его потомков</li>
                </ul>
                
                <h4>✏️ Редактирование:</h4>
                <ul>
                    <li><strong>Двойной клик на названии</strong> - переход в режим редактирования текста</li>
                    <li><strong>Enter</strong> - сохранить изменения</li>
                    <li><strong>Escape</strong> - отменить изменения</li>
                </ul>
                
                <h4>👁️ Предпросмотр:</h4>
                <p>Справа отображается живой предпросмотр структуры документа с автоматической нумерацией разделов.</p>
                
                <h4>➡️ Переход к следующему шагу:</h4>
                <p>Нажмите кнопку <strong>"Далее"</strong> внизу экрана, когда структура будет готова.</p>
            `
        },
        2: {
            title: 'Инструкция: Шаг 2 - Заполнение данных',
            content: `
                <h4>📝 Заполнение содержимого акта</h4>
                <p>На этом шаге вы заполняете данными все элементы, созданные на предыдущем шаге.</p>
                
                <h4>📊 Работа с таблицами:</h4>
                <ul>
                    <li><strong>Двойной клик на ячейке</strong> - редактирование содержимого</li>
                    <li><strong>Enter</strong> - сохранить и перейти к следующей ячейке</li>
                    <li><strong>Tab</strong> - перейти к следующей ячейке</li>
                    <li><strong>Выделение ячеек</strong> - клик с зажатым Ctrl для множественного выбора</li>
                    <li><strong>Правый клик</strong> - объединить/разъединить выбранные ячейки</li>
                    <li><strong>Изменение размера</strong> - потяните за правый край ячейки для изменения ширины столбца</li>
                </ul>
                
                <h4>📄 Текстовые блоки:</h4>
                <ul>
                    <li><strong>Форматирование текста</strong> - используйте панель инструментов (жирный, курсив, подчёркивание)</li>
                    <li><strong>Выравнивание</strong> - выберите выравнивание текста (слева, по центру, справа)</li>
                    <li><strong>Размер шрифта</strong> - настройте размер текста</li>
                </ul>
                
                <h4>⚠️ Нарушения:</h4>
                <ul>
                    <li><strong>Заполните все поля</strong> - нарушено, установлено, описание, причины, последствия, ответственные</li>
                    <li><strong>Кнопки +/-</strong> - добавление и удаление полей описания, причин и последствий</li>
                </ul>
                
                <h4>💾 Сохранение:</h4>
                <ul>
                    <li><strong>Выбор форматов</strong> - нажмите на стрелку рядом с кнопкой "Создать акт"</li>
                    <li><strong>Создать акт</strong> - генерация документа в выбранных форматах</li>
                    <li><strong>Горячая клавиша</strong> - <code>Ctrl+S</code> для быстрого сохранения</li>
                </ul>
                
                <h4>⬅️ Возврат:</h4>
                <p>Кнопка <strong>"Назад"</strong> позволяет вернуться к редактированию структуры.</p>
            `
        }
    };

    /**
     * Инициализирует менеджер помощи и устанавливает обработчики событий
     * Привязывает открытие модального окна к кнопке помощи и настраивает способы закрытия
     */
    static init() {
        // Получаем элементы DOM для работы с модальным окном
        const helpBtn = document.getElementById('helpBtn');
        const modal = document.getElementById('helpModal');

        // Прерываем инициализацию, если обязательные элементы отсутствуют
        if (!helpBtn || !modal) {
            console.error('HelpManager: не найдены необходимые элементы');
            return;
        }

        // Устанавливаем обработчик открытия модального окна по клику на кнопку помощи
        helpBtn.addEventListener('click', () => {
            this.show();
        });

        // Устанавливаем обработчик закрытия модального окна при клике на затемнённый фон
        modal.addEventListener('click', (e) => {
            if (e.target === modal) {
                this.hide();
            }
        });

        // Устанавливаем обработчик закрытия модального окна по нажатию клавиши Escape
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && !modal.classList.contains('hidden')) {
                this.hide();
            }
        });

        // Устанавливаем начальную подсказку для кнопки помощи
        this.updateTooltip();
    }

    /**
     * Открывает модальное окно с инструкцией для текущего шага работы
     * Загружает содержимое инструкции из словаря instructions на основе текущего шага
     */
    static show() {
        // Определяем текущий шаг из глобального состояния приложения
        const currentStep = AppState.currentStep || 1;
        const instruction = this.instructions[currentStep];

        // Проверяем наличие инструкции для текущего шага
        if (!instruction) {
            console.error('HelpManager: инструкция не найдена для шага', currentStep);
            return;
        }

        // Получаем элементы модального окна для заполнения контентом
        const modal = document.getElementById('helpModal');
        const title = document.getElementById('helpModalTitle');
        const body = document.getElementById('helpModalBody');

        // Заполняем модальное окно заголовком и содержимым инструкции
        title.textContent = instruction.title;
        body.innerHTML = instruction.content;

        // Отображаем модальное окно на экране
        modal.classList.remove('hidden');
    }

    /**
     * Скрывает модальное окно с инструкцией
     * Добавляет класс hidden для визуального скрытия модального окна
     */
    static hide() {
        const modal = document.getElementById('helpModal');
        modal.classList.add('hidden');
    }

    /**
     * Обновляет текст всплывающей подсказки кнопки помощи в соответствии с текущим шагом
     * Вызывается при инициализации и при смене шага работы с приложением
     */
    static updateTooltip() {
        const helpBtn = document.getElementById('helpBtn');

        // Прерываем выполнение, если кнопка помощи отсутствует
        if (!helpBtn) return;

        // Определяем текущий шаг из глобального состояния приложения
        const currentStep = AppState.currentStep || 1;

        // Словарь названий шагов для формирования подсказки
        const stepNames = {
            1: 'Составление структуры акта',
            2: 'Заполнение данных'
        };

        // Устанавливаем текст подсказки с указанием номера и названия текущего шага
        helpBtn.title = `Инструкция: Шаг ${currentStep} - ${stepNames[currentStep]}`;
    }
}
